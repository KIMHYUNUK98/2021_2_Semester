#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
    int i = 0;
    int x = 0;
    int sum = 0;
    double average = 0.F;
    int sum_avg = 0;

    srand(time(NULL));
    for(i = 0; i < 20; i++){
        x = rand() % 100 + 1
        sum += x;
    }

    average = sum / 3.0;

    for(int i = 0 ;i < 20 ; i++) {
        sum_avg = 
    }
    printf("RAND_MAX = %d\n", RAND_MAX);

    return 0;
}